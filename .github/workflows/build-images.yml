name: Build and Push Docker Images

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-backend:
    name: Build Backend Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./workshop-backend
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ozzon42/ozzon-images:api-latest
            ozzon42/ozzon-images:migrate-latest
          cache-from: type=registry,ref=ozzon42/ozzon-images:buildcache-api
          cache-to: type=registry,ref=ozzon42/ozzon-images:buildcache-api,mode=max

  build-frontend:
    name: Build Frontend Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./workshop-frontend
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ozzon42/ozzon-images:web-latest
          cache-from: type=registry,ref=ozzon42/ozzon-images:buildcache-web
          cache-to: type=registry,ref=ozzon42/ozzon-images:buildcache-web,mode=max
